name: xintcopy
on:
   workflow_dispatch:
env: 
  GH_TOKEN: ${{secrets.GH_TOKEN}}
  WEBSITE: 'biraradamoda'
  maxconcurrency: '5'
  marka: 'xint'
jobs:
  kadin_job:
    uses: ./.github/workflows/reusable.yml
    with:
      GENDER: kadin
      maxconcurrency: ${{ vars.maxconcurrency}}
      WEBSITE: ${{ vars.WEBSITE }}
      marka: ${{ vars.marka }}
    secrets: inherit
  erkek_job:
    uses: ./.github/workflows/reusable.yml
    with:
      GENDER: erkek
      maxconcurrency: ${{ vars.maxconcurrency}}
      WEBSITE: ${{ vars.WEBSITE }}
      marka: ${{ vars.marka }}
    secrets: inherit